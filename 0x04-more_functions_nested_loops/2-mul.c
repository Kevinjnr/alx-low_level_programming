#include "main.h"
/**
 * mul - multiplies two integer
 * @d: the first number to multiply
 * @x: the second number to multiply with the first numb
 * Return: the result of multiplication between d and x param
 */

int mul(int d, int x)
{
	return (d * x);
}
