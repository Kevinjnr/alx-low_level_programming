#include <stdio.h>

/**
 * main - Entry point
 * Return: 0 (success)
 */

int main(void)
{
	int y;

	char g;

	g = 'a';
	y = 0;
	while
		(y < 10) {
			putchar(y + '0');
			y++;
		}
	while
		(g <= 'f') {
			putchar(g);
			g++;
		}
	putchar('\n');
	return (0);
}
